// Polished-system shapes: pill search (18px), example cards (14px),
// POS tags (6px), pills/circular controls (full).

export const radius = {
  tag: 6,
  card: 14,
  input: 18,
  full: 9999, // pills + circular buttons only
} as const;
