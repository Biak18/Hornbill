// Design-system entry point (skill 12.1: import from design-system folder).
// App code imports UI primitives from "@/components/ui", never directly
// from third-party pressable/animation packages for press feedback.

export { PressableScale } from "./pressable-scale";
export { Card, CardTitle, CardBody } from "./card";
